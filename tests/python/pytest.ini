[pytest]
# Run from gpusim2grid/ with:
#   pytest tests/python/
#
# Or from this directory:
#   pytest .
minversion = 7.0
addopts = -v --tb=short
markers =
    gpu: requires a CUDA GPU and the gpusim2grid C extension
    slow: test is computationally expensive (>10 s)
